/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['avatars.githubusercontent.com','lh3.googleusercontent.com'], // Add GitHub or any other external domains here
    },
};

export default nextConfig;
